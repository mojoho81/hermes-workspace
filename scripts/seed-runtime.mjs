/**
 * Retired: this one-off April seed targeted the obsolete ~/.claude profile layout
 * and bypassed the workspace's exclusive atomic mode-0600 runtime writer.
 * Runtime state must now be created through the workspace swarm runtime APIs.
 */
throw new Error('seed-runtime.mjs is retired; use the workspace swarm runtime APIs')
